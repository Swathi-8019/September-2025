class Solution:
    def hammingWeight(self, n: int) -> int:
        binn = bin(n)[2:]
        count = 0
        for i in binn:
            if int(i) ^ 0 == 1:
                count += 1 
        return count
